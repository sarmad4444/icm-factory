[project]
name = "icm-generated"
version = "0.1.0"
description = "Interpretable Context Methodology (ICM) Master Workspace Builder"
readme = "AGENT.md"
requires-python = ">=3.11"
dependencies = [
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
