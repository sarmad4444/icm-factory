[project]
name = "icm-factory"
version = "0.5.0"
description = "ICM Factory — Master Control Plane & Workspace Builder for Interpretable Context Methodology"
readme = "AGENT.md"
requires-python = ">=3.11"
dependencies = [
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
