# Scaffolding Standards (Layer 3)

- Always create `output/.gitkeep` for each stage so empty output directories are tracked in git.
- Ensure strict 2-digit zero padding (`01_`, `02_`, etc.).
- Ensure all stage `CONTEXT.md` files include exact `## Inputs`, `## Process`, and `## Outputs` markdown headings.
